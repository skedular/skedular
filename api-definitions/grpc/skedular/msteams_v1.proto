syntax = "proto3";
package msteams;

option csharp_namespace = "Api.Shared.Services.Grpc.Skedular.MsTeams.V1";

service MsTeamsService {
  rpc GetVersion (VersionInput) returns (Version);
}

message VersionInput {
}

message Version {
  int32 major = 1;
  int32 minor = 2;
  int32 build = 3;
  int32 revision = 4;
}
