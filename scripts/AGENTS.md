# Scripts Notes

This file applies to `scripts`.

## Scope

- This directory owns repo-level shell entry points such as GraphQL generation, formatting, linting, and dependency
  startup.
- Event metadata is no longer generated by a script in this directory.
