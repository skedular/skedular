subscription OnResourceAvailabilityChanged($subscriptionKey: String!, $filter: ResourceAvailabilityFilterInput!) {
    resourceAvailability(subscriptionKey: $subscriptionKey, filter: $filter) {
        subscriptionKey
        items {
            resourceId
            resourceName
            status
        }
    }
}
