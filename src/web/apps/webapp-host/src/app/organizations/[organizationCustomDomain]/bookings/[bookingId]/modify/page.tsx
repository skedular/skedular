'use client';

import Page from '@/rootPages/organizations/organization/bookings/booking/page';
import { memo } from 'react';

const RootPage = () => <Page forceModify />;

export default memo(RootPage);
