'use client';

import Page from '@/rootPages/organizations/organization/locations/location/resources/add/page';
import { memo } from 'react';

const RootPage = () => <Page />;

export default memo(RootPage);
