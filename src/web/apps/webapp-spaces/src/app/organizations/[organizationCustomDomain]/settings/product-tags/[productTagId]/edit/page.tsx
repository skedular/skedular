'use client';

import Page from '@/rootPages/organizations/organization/settings/productTags/edit/page';
import { memo } from 'react';

const RootPage = () => <Page />;

export default memo(RootPage);
