'use client';

import Page from '@/rootPages/organizations/organization/settings/productTags/add/page';
import { memo } from 'react';

const RootPage = () => <Page />;

export default memo(RootPage);
