{
	"name": "cdn-r2-worker-staging",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"build": "wrangler build",
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.18.6",
		"typescript": "^6.0.3",
		"vitest": "~4.1.10",
		"wrangler": "^4.112.0"
	}
}
