schema {
  query: Query
}

type Query {
  msTeamsVersion: Version!
}

type Version @shareable {
  major: Int!
  minor: Int!
  build: Int!
  revision: Int!
}

directive @shareable repeatable on OBJECT | FIELD_DEFINITION
