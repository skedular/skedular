# Marketplace API Agent Notes

This file covers `marketplace/apis/`.

## Agent Rule

- Keep API endpoints thin.
- If the issue is about pricing or listing semantics, the fix likely belongs in shared or domain logic.
