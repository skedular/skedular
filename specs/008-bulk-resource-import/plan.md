# Implementation Plan: Bulk Resource Import

**Branch**: `008-bulk-resource-import` | **Date**: 2026-05-13 | **Spec**: [spec.md](spec.md)
**Input**: Feature specification from `specs/008-bulk-resource-import/spec.md`

## Summary

Allow location administrators to add multiple resources (desks, meeting rooms, etc.) to a
location in a single batch operation via a new `bulkImportResources` GraphQL mutation in the
`location` domain. Each batch row specifies a resource type, optional base name, quantity, and
tags; the server auto-generates individual resource names using a `{baseName}-{n}` suffix
strategy. The mutation supports partial success (valid rows persist regardless of failed rows).
A new "Bulk add" dialog in the webapp UI completes the end-to-end flow.

## Technical Context

**Language/Version**: C# .NET 10 (backend); TypeScript 6 / React 19 / Next.js 16 App Router (frontend)
**Primary Dependencies**: HotChocolate (GraphQL), Entity Framework Core, `Enterprise.Shared.Database` repository pattern, Relay, MUI v9, `mui-rff` Autocomplete
**Storage**: PostgreSQL via EF Core — no new migration; reuses existing `Resource` and `OrganizationTagResource` tables
**Testing**: `xunit` + FakeItEasy + AutoFakeItEasyData (unit); Aspire-hosted integration tests in `Location.Domain.IntegrationTests`; Vitest + React Testing Library (UI)
**Target Platform**: .NET 10 server; Next.js 16 App Router web app
**Project Type**: Web service (GraphQL API) + web application (React/Next.js)
**Performance Goals**: Batch of 100 resources completes within 5 seconds under normal load
**Constraints**: Max 100 total resources per request (sum of quantities); single DB transaction for all successful rows
**Scale/Scope**: Admin-facing feature; low frequency; up to 100 resources per import

## Constitution Check

_GATE: Must pass before Phase 0 research. Re-check after Phase 1 design._

- [x] **I. Contract-First** — Yes, this feature adds new GraphQL types to the `location` API.
      `scripts/generate-graphql.sh` regenerates the per-API `schema.graphql`, the composed
      gateway schema, and Relay artefacts. `web/apps/webapp/scripts/generate.sh` regenerates
      Relay TypeScript types. Both will be run after backend implementation.
- [x] **II. Domain Boundaries** — No cross-domain access. The bulk import mutation lives
      entirely within the `location` domain. No other domain's DB or internals are touched.
- [x] **III. Testing** — Unit tests planned for `BulkImportResourcesService` (naming logic,
      validation, partial success). Integration tests planned in
      `Location.Domain.IntegrationTests` using repository-layer assertions only (no raw
      `DbContext` in test assertions). UI tests planned with Vitest + React Testing Library.
- [x] **IV. Frontend** — Yes, UI changes in `web/apps/webapp`. Relay fragments collocated
      with their components. No hand-edited Relay artefacts. Typography via `@skedular/ui`
      wrappers. British spelling in all user-facing copy.
- [x] **V. Pattern Consistency** — New `IBulkImportResourcesService` follows the exact same
      service pattern as `IResourceService`. New `BulkImportResourcesInput` / payload classes
      follow the existing HotChocolate input/payload pattern in the location API. No deviation.
      One new repository method (`GetActiveNamesByLocationIdAsync`) follows the existing
      repository base pattern. No new frameworks or abstractions.
- [x] **VI. Logging** — Structured logging planned for: batch received (locationId, rowCount,
      totalQuantity), per-row failure (rowIndex, reason), and completion summary
      (created, rejected counts). All via `Microsoft.Extensions.Logging` ILogger injection,
      consistent with existing service logging in `ResourceService`.

## Project Structure

### Documentation (this feature)

```text
specs/008-bulk-resource-import/
├── plan.md              ← this file
├── spec.md
├── research.md
├── data-model.md
├── quickstart.md
├── contracts/
│   └── graphql-bulk-import-resources.md
├── checklists/
│   └── requirements.md
└── tasks.md             ← generated by /speckit.tasks (not yet created)
```

### Backend — new files

```text
location/apis/Location.Api/GraphQL/Resource/
├── BulkImportResourcesInput.cs
├── BulkImportResourceRowInput.cs
├── BulkImportResourcesPayload.cs
└── BulkImportResourceRowResult.cs

location/apis/Location.Api/Services/
└── BulkImportResourcesService.cs          (IBulkImportResourcesService + impl)

location/apis/Location.Api.UnitTests/Services/BulkImportResourcesServiceTests/
├── ImportShould.cs                         (naming logic, validation, partial success)
└── ...
```

### Backend — modified files

```text
location/apis/Location.Api/GraphQL/Resource/RootMutation.cs  (+BulkImportResourcesAsync)
location/shared/Location.Shared/Repositories/ResourceRepository.cs  (+GetActiveNamesByLocationIdAsync)
location/apis/Location.Api/Mappers/GraphQlMapper.cs          (+MapTo overloads for new types)
location/shared/Location.Shared/Mappers/EntityMapper.cs      (+bulk batch MapTo if needed)
```

### Backend — integration tests

```text
location/domain/Location.Domain.IntegrationTests/Api/GraphQL/Resource/
└── BulkImportResourcesShould.cs
```

### Frontend — new files

```text
web/apps/webapp/src/components/resource/bulkImportResources/
├── bulk-import-resources-dialog.tsx
├── bulk-import-resources-row.tsx
└── index.ts
```

### Frontend — modified files

```text
web/apps/webapp/src/components/organization/organizationLocation/
└── organization-location-manage-resources-section.tsx  (+BulkImportResourcesButton + dialog wiring)
```

## Complexity Tracking

No constitution violations. No complexity exceptions required.

## Post-Design Constitution Check

All gates re-evaluated after Phase 1 design — no new violations found. The pre-validation
approach (validate all rows before any DB write, single transaction for successful rows)
simplifies rollback reasoning and aligns with existing single-resource `AddAsync` pattern.

## Project Structure

### Documentation (this feature)

```text
specs/[###-feature]/
├── plan.md              # This file (/speckit.plan command output)
├── research.md          # Phase 0 output (/speckit.plan command)
├── data-model.md        # Phase 1 output (/speckit.plan command)
├── quickstart.md        # Phase 1 output (/speckit.plan command)
├── contracts/           # Phase 1 output (/speckit.plan command)
└── tasks.md             # Phase 2 output (/speckit.tasks command - NOT created by /speckit.plan)
```

### Source Code (repository root)

<!--
  ACTION REQUIRED: Replace the placeholder tree below with the concrete layout
  for this feature. Delete unused options and expand the chosen structure with
  real paths (e.g., apps/admin, packages/something). The delivered plan must
  not include Option labels.
-->

```text
# [REMOVE IF UNUSED] Option 1: Single project (DEFAULT)
src/
├── models/
├── services/
├── cli/
└── lib/

tests/
├── contract/
├── integration/
└── unit/

# [REMOVE IF UNUSED] Option 2: Web application (when "frontend" + "backend" detected)
backend/
├── src/
│   ├── models/
│   ├── services/
│   └── api/
└── tests/

frontend/
├── src/
│   ├── components/
│   ├── pages/
│   └── services/
└── tests/

# [REMOVE IF UNUSED] Option 3: Mobile + API (when "iOS/Android" detected)
api/
└── [same as backend above]

ios/ or android/
└── [platform-specific structure: feature modules, UI flows, platform tests]
```

**Structure Decision**: [Document the selected structure and reference the real
directories captured above]

## Complexity Tracking

> **Fill ONLY if Constitution Check has violations that must be justified**

| Violation                  | Why Needed         | Simpler Alternative Rejected Because |
| -------------------------- | ------------------ | ------------------------------------ |
| [e.g., 4th project]        | [current need]     | [why 3 projects insufficient]        |
| [e.g., Repository pattern] | [specific problem] | [why direct DB access insufficient]  |
