# Implementation Plan: Reliable Marketplace Booking Cleanup

**Branch**: `044-marketplace-booking-cleanup` | **Date**: 2026-08-29 | **Spec**: [spec.md](spec.md)

## Summary

Make local resource release the authoritative, transactional boundary for marketplace booking failure cleanup. Existing payment and invoice workflows will detect terminal failures and delegate to a shared Temporal cleanup contract that performs local release first; provider cleanup remains independently retryable. Extend existing Booking.Shared workflows, activities, repositories, failure records, and reconciliation service so provider outages cannot strand resources; expose truthful release/accounting states through existing API and Relay surfaces.

## Technical Context

**Language/Version**: C# .NET 10; TypeScript 6 / React 19 / Next.js 16
**Primary Dependencies**: Temporal workflows, EF Core/PostgreSQL repositories, Stripe and Xero integrations, HotChocolate/Fusion GraphQL, Relay 21, existing outbox/event infrastructure
**Storage**: Booking-owned PostgreSQL persistence; extend marketplace failure/cleanup and accounting transition records as needed
**Testing**: xUnit, AutoFixture/FakeItEasy `AutoFakeItEasyData` theories, Vitest/React Testing Library; integration tests only where unit tests cannot prove essential merge safety
**Target Platform**: Cloud-hosted Booking API, workers/jobs, and customer/operator web applications
**Project Type**: Full-stack booking domain reliability feature
**Performance Goals**: Local release uses at most five delayed/exponential-backoff Temporal retries; retry exhaustion immediately creates a reconciliation candidate, while recurring reconciliation processes remaining eligible terminal records within one configured run interval
**Constraints**: No direct EF from workflows/activities/API/tests where prohibited; required parameters precede `CancellationToken`; generated contracts are regenerated, never hand-edited; American English copy
**Scale/Scope**: Newly handled one-time and recurring marketplace bookings, subscription-linked payment ownership, and all listed payment/invoice failure paths

## Constitution Check

_Gates pass._

- [x] **I. Contract-First** — GraphQL/UI status fields may change. Update source schema only, then run `scripts/generate-graphql.sh` and Relay generation; do not edit generated output.
- [x] **II. Domain Boundaries** — Keep persistence in Booking repositories/services; workflows and transport layers call service contracts. Resolve linked subscription/payment ownership through Booking-owned services. Use explicit mappings for persisted states.
- [x] **III. Testing** — Unit-test every changed workflow/activity/service first; integration-test transaction, concurrency, repository, migration, and provider boundary behavior only where unit tests cannot prove it.
- [x] **IV. Frontend** — Return stable IDs and rendered status fields in mutation payloads; use Relay normalization/connection updates or targeted refetches, never reload. Use typography wrappers and review public documentation for changed behavior.
- [x] **V. Pattern Consistency** — Reuse existing failure entities, Temporal outbox signals, repository/unit-of-work boundaries, and reconciliation hosted-service patterns. The local-release/accounting-transition separation removes provider ordering coupling.
- [x] **VI. Logging** — Add structured logs for local release, provider transition, retries, reconciliation, and status publication with correlation identifiers and no sensitive payment data.

## Project Structure

### Documentation

```text
specs/044-marketplace-booking-cleanup/
├── plan.md
├── research.md
├── data-model.md
├── quickstart.md
├── contracts/cleanup-reliability.md
└── tasks.md                 # generated by /speckit-tasks
```

### Source Code

```text
src/booking/shared/Booking.Shared/
├── Activities/BookingIntegrations.cs
├── Activities/MarketplaceBookingSubscriptionIntegrations.cs
├── Workflows/PayBookingViaCard.cs
├── Workflows/PayRecurringBookingViaCard.cs
├── Workflows/PayBookingViaBankTransfer.cs
├── Workflows/PayRecurringBookingViaBankTransfer.cs
├── Workflows/GenerateInitialArrearsBookingInvoice.cs
├── Workflows/GenerateInitialArrearsRecurringBookingInvoice.cs
├── Services/MarketplaceBookingFailureService.cs
├── Repositories/MarketplaceBookingFailureRepository.cs
└── Database/Entities/MarketplaceBookingFailure*.cs
src/booking/jobs/Booking.Jobs/Services/MarketplaceRefundReconciliationHostedService.cs
src/booking/apis/Booking.Api/{schema.graphqls,Services/MarketplaceBookingFailureReadService.cs}
src/booking/domain/Booking.Domain.IntegrationTests/{Activities,Repositories,Services}
src/web/apps/webapp*/                 # existing marketplace failure/status views and Relay artifacts
```

**Structure Decision**: Extend the existing Booking domain and job/API/UI surfaces; do not introduce a new service boundary or customer-data store.

## Complexity Tracking

No constitution violations.
