# Contracts Overview

This directory captures implementation contracts for split UI scaffolding.

## Files

- `webapp-structure.md`: app and health project layout contract.
- `terraform-structure.md`: workspace and backend key contract.
- `github-actions-workflows.md`: CI workflow and validation contract.

## Usage

Use these contracts as source-of-truth while executing tasks in `tasks.md`.
