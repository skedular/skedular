# Environment Variable Contract

All split web products must keep equivalent environment key sets for shared behaviour.

## Core keys

- `NEXT_PUBLIC_API_URL`
- `NEXT_PUBLIC_APP_NAME`
- `NEXT_PUBLIC_APP_ENV`
- shared telemetry keys (when enabled)
