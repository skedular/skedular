{
  "name": "skedular-event-catalog",
  "version": "0.1.0",
  "description": "Skedular Event Catalog",
  "private": true,
  "scripts": {
    "dev": "cross-env PORT=15003 eventcatalog dev",
    "build": "eventcatalog build",
    "start": "eventcatalog start",
    "preview": "eventcatalog preview",
    "generate": "eventcatalog generate",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@eventcatalog/core": "2.23.3"
  },
  "devDependencies": {
    "@astrojs/mdx": "^4.0.8",
    "@parcel/watcher": "2.5.1",
    "@types/lodash.merge": "4.6.9",
    "concurrently": "9.1.2",
    "cross-env": "7.0.3"
  }
}
