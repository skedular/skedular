'use client';

import Page from '@/rootPages/organizations/organization/locations/location/resources/resource/page';
import { memo } from 'react';

const RootPage = () => <Page />;

export default memo(RootPage);
