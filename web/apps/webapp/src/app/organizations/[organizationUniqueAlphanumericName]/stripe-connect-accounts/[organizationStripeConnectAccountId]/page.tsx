'use client';

import Page from '@/rootPages/organizations/organization/stripe-connect-accounts/organizationStripeConnectAccount/page';
import { memo } from 'react';

const RootPage = () => <Page />;

export default memo(RootPage);
