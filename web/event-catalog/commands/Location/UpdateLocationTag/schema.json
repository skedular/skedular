{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AddTeamCommand",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "The unique identifier of the location tag being added to the organization."
    },
    "name": {
      "type": "string",
      "description": "The name of the location tag being updated in an organization."
    }
  },
  "required": [
    "id",
    "name",
    "about"
  ],
  "additionalProperties": true
}