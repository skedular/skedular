//----------------------
// <auto-generated>
// Generated using Skedularctl v1.0.0.0. DO NOT EDIT!
// </auto-generated>
//----------------------

using System.Text.RegularExpressions;
using Enterprise.Shared.Events;
using Google.Protobuf.WellKnownTypes;

#nullable enable

namespace Api.Shared.Clients.Events.Skedular.Customer.V1.Value;

public partial class Metadata : IMetadata { }

[KafkaTopic(
    topicName: "customer.v1.event",
    topicPartitionCount: 3,
    retryTopicNamePrefix: "customer.v1.event.retry",
    retryTopicCount: 1,
    retryTopicPartitionCount: 3,
    deadLetterTopicName: "customer.v1.event.deadletter",
    deadLetterTopicPartitionCount: 3,
    protobufSchema: "syntax = \"proto3\";package customer;import \"google/protobuf/timestamp.proto\";option csharp_namespace = \"Api.Shared.Clients.Events.Skedular.Customer.V1.Value\";enum Type {  CustomerUpserted = 0;  CustomerDeleted = 1;}message Event {  Metadata metadata = 1;  Data data = 2;}message Metadata {  string id = 1;  string domainSource = 2;  string appSource = 3;  Type type = 4;  google.protobuf.Timestamp time = 5;  string correlationId = 6;}message Data {   Customer customer = 1; }message Customer {  string id = 1;  google.protobuf.Timestamp deletedAt = 2;  repeated Identity identities = 3;  string designation = 4;  string title = 5;  string name = 6;  string givenName = 7;  string middleName = 8;  string familyName = 9;  string photoUrl = 10;  string photoUrl24 = 11;  string photoUrl32 = 12;  string photoUrl48 = 13;  string photoUrl72 = 14;  string photoUrl192 = 15;  string photoUrl512 = 16;  string timezone = 17;  string locale = 18;  repeated Location preferredLocations = 19;  string preferredOrganizationId = 20;  repeated Resource preferredResources = 21;  Settings settings = 22;  repeated Team preferredTeams = 23;  repeated OrganizationTag preferredOrganizationTags = 24;  string phoneNumber = 25;  CustomerBillingDetails billingDetails = 26;}message Identity {  string id = 1;  string email = 2;  bool emailVerified = 3;}message Settings {  string id = 1;  bool isOrganizationOnboardingDone = 2;  bool isLocationOnboardingDone = 3;  bool isTeamOnboardingDone = 4;  bool isDefaultOrganizationOnboardingDone = 5;  bool isPreferredLocationOnboardingDone = 6;  bool isPreferredZoneOnboardingDone = 7;}message Location {  string id = 1;  string organizationId = 2;}message Team {  string id = 1;  string organizationId = 2;}message Resource {  string id = 1;  string locationId = 2;}message OrganizationTag {  string id = 1;  string organizationId = 2;}message CustomerBillingDetails {  string id = 1;  string companyName = 2;  string email = 3;  string addressLine1 = 4;  string addressLine2 = 5;  string suburb = 6;  string city = 7;  string province = 8;  string zipcode = 9;  string country = 10;}")]
public partial class Event : IMetadataEvent
{
    private static readonly Regex ValidKafkaTopicCharacters =
        new(@"^[a-z0-9\.\-_]+$", RegexOptions.IgnoreCase | RegexOptions.Singleline | RegexOptions.Compiled);

    private const string TopicName = "customer.v1.event";
    private const string RetryTopicNamePrefix = "customer.v1.event.retry";
    private const int RetryTopicCount = 1;
    private const string DeadLetterTopicName = "customer.v1.event.deadletter";

    public static Metadata NewMetadata(
        string domainSource,
        string appSource,
        Type type,
        string? correlationId,
        Guid? id = null) =>
        new() {
            Id = id.HasValue ? id.Value.ToString() : Guid.CreateVersion7().ToString(),
            DomainSource = domainSource,
            AppSource = appSource,
            Type = type,
            Time = Timestamp.FromDateTimeOffset(TimeProvider.System.GetUtcNow()),
            CorrelationId = string.IsNullOrWhiteSpace(correlationId) ? Guid.CreateVersion7().ToString() : correlationId,
    };

    public string GetTopicName(string prefix)
    {
        var name = string.IsNullOrWhiteSpace(prefix) ? TopicName : $"{prefix}.{TopicName}";
        if (ValidKafkaTopicCharacters.IsMatch(name))
        {
            return name;
        }

        throw new ArgumentException($"Invalid characters in topic name `{name}`", nameof(prefix));
    }

    public int GetRetryTopicCount() => RetryTopicCount;

    public string GetRetryTopicName(string prefix, int idx)
    {
        var name = string.IsNullOrWhiteSpace(prefix) ? $"{RetryTopicNamePrefix}.{idx}" : $"{prefix}.{RetryTopicNamePrefix}.{idx}";
        if (ValidKafkaTopicCharacters.IsMatch(name))
        {
            return name;
        }

        throw new ArgumentException($"Invalid characters in topic name `{name}`", nameof(prefix));
    }

    public string GetDeadLetterTopicName(string prefix)
    {
        var name = string.IsNullOrWhiteSpace(prefix) ? DeadLetterTopicName : $"{prefix}.{DeadLetterTopicName}";
        if (ValidKafkaTopicCharacters.IsMatch(name))
        {
            return name;
        }

        throw new ArgumentException($"Invalid characters in topic name `{name}`", nameof(prefix));
    }

    public string? GetCorrelationId() => Metadata.CorrelationId;
}
