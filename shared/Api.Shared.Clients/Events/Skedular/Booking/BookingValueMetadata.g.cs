//----------------------
// <auto-generated>
// Generated using Skedularctl v1.0.0.0. DO NOT EDIT!
// </auto-generated>
//----------------------

using System.Text.RegularExpressions;
using Enterprise.Shared.Events;
using Google.Protobuf.WellKnownTypes;

#nullable enable

namespace Api.Shared.Clients.Events.Skedular.Booking.V1.Value;

public partial class Metadata : IMetadata { }

[KafkaTopic(
    topicName: "booking.v1.event",
    topicPartitionCount: 3,
    retryTopicNamePrefix: "booking.v1.event.retry",
    retryTopicCount: 1,
    retryTopicPartitionCount: 3,
    deadLetterTopicName: "booking.v1.event.deadletter",
    deadLetterTopicPartitionCount: 3,
    protobufSchema: "syntax = \"proto3\";package booking;import \"google/protobuf/timestamp.proto\";option csharp_namespace = \"Api.Shared.Clients.Events.Skedular.Booking.V1.Value\";enum Type {  Type_BookingUpserted = 0;  Type_BookingDeleted = 1;}message Event {  Metadata metadata = 1;  Data data = 2;}message Metadata {  string id = 1;  string domainSource = 2;  string appSource = 3;  Type type = 4;  google.protobuf.Timestamp time = 5;  string correlationId = 6;}message Data { Booking booking = 1; }message Booking {  string id = 1;  google.protobuf.Timestamp deletedAt = 2;  google.protobuf.Timestamp from = 3;  google.protobuf.Timestamp until = 4;  string notes = 5;  BookingType type = 6;  PaymentStatus paymentStatus = 7;  bool isPaymentRequired = 8;  repeated string involvedCustomerIds = 9;  repeated string involvedOrganizationIds = 10;  repeated string involvedLocationIds = 11;  repeated string involvedTeamIds = 12;  repeated Resource resources = 13;  repeated BookingSchedule Schedules = 14;  string paidByCustomerId = 15;  string paidByOrganizationId = 16;  string createdByCustomerId = 17;  string lastModifiedByCustomerId = 18;  string deletedByCustomerId = 19;  repeated LineItem lineItems = 20;  BookingCheckoutSession bookingCheckoutSession = 21;  bool bookedOnMarketplace = 22;  PaymentMethod PaymentMethod = 23;  string totalAmount = 24;  string currency = 25;  string InvoiceUrl = 26;  string InvoiceNumber = 27;  repeated string InvoiceEmailList = 28;}enum BookingType{  BookingType_WorkingFromHome = 0;  BookingType_WorkingFromOffice = 1;  BookingType_WorkingFromCoworkingSpace = 2;  BookingType_SickLeave = 3;  BookingType_AnnualLeave = 4;  BookingType_WellbeingLeave = 5;  BookingType_ClientOffice = 6;  BookingType_Vacation = 7;  BookingType_TravelingForWork = 8;  BookingType_NonWorkingDay = 9;}message Resource {  string id = 1;  repeated string customerIds = 2;}message BookingSchedule {  google.protobuf.Timestamp from = 1;  google.protobuf.Timestamp until = 2;}enum PaymentStatus{  PaymentStatus_Pending = 0;  PaymentStatus_Rejected = 1;  PaymentStatus_Confirmed = 2;  PaymentStatus_Expired = 3;  PaymentStatus_RecordNeverCreated = 4;  PaymentStatus_NoPaymentRequired = 5;}message LineItem {  string productVersionId = 1;  int32 quantity = 2;}message BookingCheckoutSession {  string id = 1;  string checkoutUrl = 2;}enum PaymentMethod {  PaymentMethod_Card = 0;  PaymentMethod_BankAccount = 1;}")]
public partial class Event : IMetadataEvent
{
    private static readonly Regex ValidKafkaTopicCharacters =
        new(@"^[a-z0-9\.\-_]+$", RegexOptions.IgnoreCase | RegexOptions.Singleline | RegexOptions.Compiled);

    private const string TopicName = "booking.v1.event";
    private const string RetryTopicNamePrefix = "booking.v1.event.retry";
    private const int RetryTopicCount = 1;
    private const string DeadLetterTopicName = "booking.v1.event.deadletter";

    public static Metadata NewMetadata(
        string domainSource,
        string appSource,
        Type type,
        string? correlationId,
        Guid? id = null) =>
        new() {
            Id = id.HasValue ? id.Value.ToString() : Guid.CreateVersion7().ToString(),
            DomainSource = domainSource,
            AppSource = appSource,
            Type = type,
            Time = Timestamp.FromDateTimeOffset(TimeProvider.System.GetUtcNow()),
            CorrelationId = string.IsNullOrWhiteSpace(correlationId) ? Guid.CreateVersion7().ToString() : correlationId,
    };

    public string GetTopicName(string prefix)
    {
        var name = string.IsNullOrWhiteSpace(prefix) ? TopicName : $"{prefix}.{TopicName}";
        if (ValidKafkaTopicCharacters.IsMatch(name))
        {
            return name;
        }

        throw new ArgumentException($"Invalid characters in topic name `{name}`", nameof(prefix));
    }

    public int GetRetryTopicCount() => RetryTopicCount;

    public string GetRetryTopicName(string prefix, int idx)
    {
        var name = string.IsNullOrWhiteSpace(prefix) ? $"{RetryTopicNamePrefix}.{idx}" : $"{prefix}.{RetryTopicNamePrefix}.{idx}";
        if (ValidKafkaTopicCharacters.IsMatch(name))
        {
            return name;
        }

        throw new ArgumentException($"Invalid characters in topic name `{name}`", nameof(prefix));
    }

    public string GetDeadLetterTopicName(string prefix)
    {
        var name = string.IsNullOrWhiteSpace(prefix) ? DeadLetterTopicName : $"{prefix}.{DeadLetterTopicName}";
        if (ValidKafkaTopicCharacters.IsMatch(name))
        {
            return name;
        }

        throw new ArgumentException($"Invalid characters in topic name `{name}`", nameof(prefix));
    }

    public string? GetCorrelationId() => Metadata.CorrelationId;
}
