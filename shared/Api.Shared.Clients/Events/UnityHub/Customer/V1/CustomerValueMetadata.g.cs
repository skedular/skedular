//----------------------
// <auto-generated>
// Generated using Unityhubctl v1.0.0.0. DO NOT EDIT!
// </auto-generated>
//----------------------

using System.Text.RegularExpressions;

using Api.Shared.Events;

using Google.Protobuf.WellKnownTypes;

using NanoidDotNet;

#nullable enable

namespace Api.Shared.Clients.Events.UnityHub.Customer.V1.Value;

public partial class Metadata : IMetadata { }

[KafkaTopic(
    topicName: "customer.v1.event",
    topicPartitionCount: 5,
    retryTopicNamePrefix: "customer.v1.event.retry",
    retryTopicCount: 1,
    retryTopicPartitionCount: 5,
    deadLetterTopicName: "customer.v1.event.deadletter",
    deadLetterTopicPartitionCount: 3,
    protobufSchema: "syntax = \"proto3\";package customer;import \"google/protobuf/timestamp.proto\";option csharp_namespace = \"Api.Shared.Clients.Events.UnityHub.Customer.V1.Value\";enum Type {  CustomerUpserted = 0;  CustomerDeleted = 1;}message Event {  Metadata metadata = 1;  Data data = 2;}message Metadata {  string id = 1;  string domainSource = 2;  string appSource = 3;  Type type = 4;  google.protobuf.Timestamp time = 5;  string correlationId = 6;}message Data { Customer afterState = 1; }message Customer {  string id = 1;  google.protobuf.Timestamp deletedAt = 2;  repeated Identity identities = 3;  string designation = 4;  string title = 5;  string name = 6;  string givenName = 7;  string middleName = 8;  string familyName = 9;  string photoUrl = 10;  string photoUrl24 = 11;  string photoUrl32 = 12;  string photoUrl48 = 13;  string photoUrl72 = 14;  string photoUrl192 = 15;  string photoUrl512 = 16;  string timezone = 17;  string locale = 18;  repeated Location defaultLocations = 19;  string defaultOrganizationId = 20;  repeated LocationTag defaultLocationTags = 21;  repeated Desk defaultDesks = 22;  Settings settings = 23;  repeated Team defaultTeams = 24;}message Identity {  string id = 1;  string email = 2;  bool emailVerified = 3;}message Settings {  string id = 1;  bool isOrganizationOnboardingDone = 2;  bool isLocationOnboardingDone = 3;  bool isDefaultOrganizationOnboardingDone = 4;  bool isDefaultLocationOnboardingDone = 5;  bool isPreferredZoneOnboardingDone = 6;  bool isPreferredDeskOnboardingDone = 7;}message Location {  string id = 1;  string organizationId = 2;}message Team {  string id = 1;  string organizationId = 2;}message LocationTag {  string id = 1;  string locationId = 2;}message Desk {  string id = 1;  string locationId = 2;}")]
public partial class Event : IMetadataEvent
{
    private static readonly Regex ValidKafkaTopicCharacters =
        new(@"^[a-z0-9\.\-_]+$", RegexOptions.IgnoreCase | RegexOptions.Singleline | RegexOptions.Compiled);

    private const string TopicName = "customer.v1.event";
    private const string RetryTopicNamePrefix = "customer.v1.event.retry";
    private const int RetryTopicCount = 1;
    private const string DeadLetterTopicName = "customer.v1.event.deadletter";

    public static Metadata NewMetadata(
        string domainSource,
        string appSource,
        Type type,
        string correlationId,
        string? id = null) =>
        new() {
            Id = id ?? NanoidDotNet.Nanoid.Generate(),
            DomainSource = domainSource,
            AppSource = appSource,
            Type = type,
            Time = Timestamp.FromDateTime(System.DateTime.UtcNow),
            CorrelationId = correlationId,
    };

    public string GetTopicName(string prefix)
    {
        var name = string.IsNullOrWhiteSpace(prefix) ? TopicName : $"{prefix}.{TopicName}";
        if (ValidKafkaTopicCharacters.IsMatch(name))
        {
            return name;
        }

        throw new ArgumentException($"Invalid characters in topic name `{name}`", nameof(prefix));
    }

    public int GetRetryTopicCount() => RetryTopicCount;

    public string GetRetryTopicName(string prefix, int idx)
    {
        var name = string.IsNullOrWhiteSpace(prefix) ? $"{RetryTopicNamePrefix}.{idx}" : $"{prefix}.{RetryTopicNamePrefix}.{idx}";
        if (ValidKafkaTopicCharacters.IsMatch(name))
        {
            return name;
        }

        throw new ArgumentException($"Invalid characters in topic name `{name}`", nameof(prefix));
    }

    public string GetDeadLetterTopicName(string prefix)
    {
        var name = string.IsNullOrWhiteSpace(prefix) ? DeadLetterTopicName : $"{prefix}.{DeadLetterTopicName}";
        if (ValidKafkaTopicCharacters.IsMatch(name))
        {
            return name;
        }

        throw new ArgumentException($"Invalid characters in topic name `{name}`", nameof(prefix));
    }

    public string? GetCorrelationId() => Metadata.CorrelationId;
}
